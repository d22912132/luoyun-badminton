{
  "name": "luoyun-badminton",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs",
    "dev:cloud": "wrangler dev",
    "deploy": "wrangler deploy",
    "check:cloud": "wrangler deploy --dry-run",
    "test:cloud": "node test/cloud-smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  }
}
